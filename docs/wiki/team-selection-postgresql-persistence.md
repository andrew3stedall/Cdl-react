# Team selection PostgreSQL persistence

Issue #66 has a PostgreSQL schema foundation for lineup slots, captaincy, vice-captaincy, bench and reserve order, chip state, fixture locks, and audit events.

Migration `0005_team_selection_persistence` follows `0004_squad_transfer_persistence`.
