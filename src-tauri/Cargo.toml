[package]
name = "cdl-tauri"
version = "0.1.0"
description = "Castle Draft League mobile shell"
authors = ["Castle Draft League"]
edition = "2021"

[lib]
name = "cdl_tauri_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
